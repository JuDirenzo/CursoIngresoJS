function mostrar()
{
/*	control = 10;

	while (control >= 1) {
		alert(control);
		control--;
	}
*/
	control = 1;
	mensaje = " ";

	while (control <= 10) {
		alert(mensaje = " " + control + mensaje);

		control ++;
	}
}