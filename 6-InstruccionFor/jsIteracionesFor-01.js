function mostrar() {

	for (let i = 1; i <= 10; i++) {
		alert(i)
	}

 /* let num;
  let acumulador = 0;
  let contador = 0;

  while (acumulador < 1000) {
    num = parseInt(prompt("Ingrese un número: "));

	acumulador += num;
	contador ++;
  }
alert("Se pidieron " + contador + " números para llegar a 1000.")*/
}
