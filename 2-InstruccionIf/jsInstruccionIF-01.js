function mostrar()
{
	let edad;
	edad = parseInt(document.getElementById("txtIdEdad").value)
	//tomo la edad  
	if (edad == 15){
		alert("niña bonita.")
		
	// el triple igual (===) respeta el tipo y el valor.
		
	}
		else (edad != 15); {
			alert("no es la niña bonita.")
		}

}//FIN DE LA FUNCIÓN